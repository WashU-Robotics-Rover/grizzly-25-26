# colcon defaults configuration
# Note: Platform-specific Python paths are handled by grizzly.py build command
# This file is kept minimal to allow cross-platform builds
# macOS builds will have Python paths set via --cmake-args in grizzly.py
# Linux builds will use system Python automatically
build:
  cmake-args: []
